# Copy to .env. Free tier: https://console.groq.com/keys
# Never commit .env.
VITE_GROQ_API_KEY=gsk_your_key_here

# Vercel (recommended): set GROQ_API_KEY in the dashboard so the serverless proxy uses a secret key.
# Optional: VITE_GROQ_CHAT_URL if you point chat/completions at another host.
